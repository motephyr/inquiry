<%# if request.original_fullpath[0..27] == "/account/user_infos/search" %>
  <% @q = UserInfo.ransack(params[:q]) %>
  <%= search_form_for @q,url: search_account_user_infos_path, :class => "navbar-form navbar-left", :method => :get do |f| %>
    <%= f.search_field :user_name_or_name_or_work_content_or_work_area_or_skill_tool_cont, class: "form-control", placeholder: '搜尋工作者名稱、地區、工作內容、技能..',style: 'width:320px' %>
  <% end %>
<%# end -%>