<% if activity.trackable %>
  <%= link_to "#{activity.owner.nickname} 新增了一個估價 #{activity.trackable.title}", activity.trackable, class: "list-group-item" %>
<% else %>
  <%= activity.owner.nickname %>
  新增了一個估價 已被刪除
<% end %>